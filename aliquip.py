votes = [5, 3, 7, 2, 6]  # Example list of vote counts
maxVote = 0  # Initialize maxVote variable

for i in range(len(votes)):
    if vote[i] > maxVote:
        maxVote = vote[i]

print("The maximum vote count is:", maxVote)
